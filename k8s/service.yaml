apiVersion: v1
kind: Service
metadata:
  name: notifications-worker
  namespace: fcgames
  labels:
    app: notifications-worker
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5004
    name: http
  selector:
    app: notifications-worker
